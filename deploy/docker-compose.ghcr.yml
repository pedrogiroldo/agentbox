# Deploy the prebuilt image — no build step on your server.
#
# This is the file to paste into Coolify / Dokploy / Portainer when you do not
# want them compiling the image on a small VPS. See docs/deploy.md.
name: agentbox

services:
  agentbox:
    image: ghcr.io/pedrogiroldo/agentbox:latest
    # `latest` moves; `docker compose up` does not follow it. Compose pulls a
    # tag only when the daemon does not already have it, so a second deploy
    # silently reuses the old image -- and since the image id is unchanged, it
    # does not even recreate the container. Dokploy deploys with
    # `up -d --build`, which is a no-op for a service without a `build:`, so
    # that is the default outcome there. Fetch on every deploy instead.
    pull_policy: always
    container_name: agentbox
    hostname: agentbox
    restart: unless-stopped
    # Stopping your containers, then saving the system layer.
    stop_grace_period: 90s

    # The box runs its own Docker daemon (installed on the first boot), and a
    # daemon needs this to run anything. It also makes the box as trusted as
    # the host — docs/security.md. Drop it and set AGENTBOX_DOCKER=off for a
    # box that cannot reach the host; everything else still works.
    privileged: true

    ports:
      - "2222:22"
      # Collie (docs/collie.md) needs nothing here: it binds loopback and the
      # box's own `tailscale serve` reaches it inside this container.

    environment:
      # REQUIRED — the public key you will SSH with (laptop and/or phone).
      SSH_PUBLIC_KEY: "ssh-ed25519 AAAA... you@laptop"
      GIT_USER_NAME: "Your Name"
      GIT_USER_EMAIL: "you@example.com"
      TZ: "UTC"
      LANG: "en_US.UTF-8"
      LC_ALL: "en_US.UTF-8"
      # install (default): the first boot fetches the Docker engine, and the
      # state volume keeps it from then on. off: no daemon, no download.
      AGENTBOX_DOCKER: "install"
      # What a client outside dials to mirror a project onto a laptop
      # (docs/mirror.md). The box cannot know its own address; leave the host
      # empty and `agentbox-mirror` prints a placeholder instead.
      AGENTBOX_SSH_HOST: ""
      AGENTBOX_SSH_PORT: "2222"
      # The herdr server starts with the box, so the herd exists before anyone
      # logs in. "0" goes back to starting it yourself from a shell.
      AGENTBOX_HERDR_SERVER: "1"
      # The box joins a tailnet as its own node, in userspace networking: no
      # TUN device, no extra capability, no published port. docs/tailscale.md.
      AGENTBOX_TAILSCALE: "off"
      # An auth key joins on the first boot with nobody watching.
      TS_AUTHKEY: ""
      TS_HOSTNAME: ""
      # Collie is in the image and does not run: off (default) | auto | on.
      # Turning it on is shell access to this box from a browser — read
      # docs/security.md, then docs/collie.md, before you change it.
      AGENTBOX_COLLIE: "off"
      COLLIE_PORT: "8787"
      # Your tailnet login, checked against the header `tailscale serve` injects.
      COLLIE_TRUSTED_USER: ""
      # Push notifications come armed: the box generates the keypair before
      # starting Collie, so all that is left is saying yes on the phone.
      AGENTBOX_COLLIE_PUSH: "1"
      COLLIE_PUSH_SUBJECT: ""

    volumes:
      - agentbox-home:/home/dev
      # apt packages and edits outside the home, replayed on every boot.
      # Deleting both volumes is what "start over" means.
      - agentbox-state:/var/lib/agentbox
      # Docker's images and containers, kept across a recreate.
      - agentbox-docker:/var/lib/docker
      # Borrow the host's daemon instead of running one here — docs/docker.md.
      # - /var/run/docker.sock:/var/run/docker.sock

volumes:
  agentbox-home:
  agentbox-state:
  agentbox-docker:
